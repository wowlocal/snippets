/// Marker exported by the target that compiles the generated OpenAPI models and
/// server protocol. The normative document lives at `api/snippets-sync-v1.yaml`;
/// a test verifies that the build-plugin input is an exact copy.
public enum SyncOpenAPIContract {
    public static let majorVersion = 1
}
